# TimingShow 1.3.2 fix2

- 判定显示替换不再只依赖 `TextMesh`，会通过反射兼容 `UnityEngine.UI.Text` 和 TextMeshPro 类文字组件。
- 判定替换从一次性改字改成短时间内兜底重写，避免新版动画帧把文字刷回原判定。
- 跟随判定颜色会优先读取 Show 参数、判定对象、实际文字组件颜色，再回退到默认判定颜色。
- 命中判定名优先从 Show 参数读取，其次从对象字段和实际显示文字读取。
