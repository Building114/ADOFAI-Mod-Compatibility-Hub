# Third-Party Notices

This source archive preserves the original KeyViewer GPL-3.0 license text in `LICENSE.md`.

The upstream KeyViewer README also identifies AdofaiTweaks and RapidGUI as MIT-licensed components. The preserved upstream text is available in `UPSTREAM_README.md`.

## NCalc

The patched project references `ncalc` version `1.3.8` through `packages.config`.

This cleaned source archive intentionally does **not** redistribute a copied `NCalc.dll` binary or the restored `packages/` folder. Restore the package before building.

Before publishing a separate player installation package that contains `NCalc.dll`, confirm the applicable redistribution terms and include the required third-party notice or license text.
